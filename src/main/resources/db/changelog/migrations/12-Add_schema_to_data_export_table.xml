<?xml version="1.1" encoding="UTF-8" standalone="no"?>
<databaseChangeLog
  xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
         http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.4.xsd">

  <changeSet id="12-1-Add_schema_to_data_export_table"  context="schema-change" author="hdosprazeres">
    <addColumn tableName="data_export">
      <column name="schema" type="jsonb">
        <constraints nullable="true"/>
      </column>
    </addColumn>
    <addColumn tableName="data_export_template">
      <column name="schema" type="jsonb">
        <constraints nullable="true"/>
      </column>
    </addColumn>
  </changeSet>

  <changeSet id="12-2-migrate-data-export-to-schema" author="hdosprazeres" context="schema-change">
    <sql>
      UPDATE data_export
      SET schema = jsonb_build_object(
      source,
      CASE
      WHEN column_aliases IS NOT NULL
      AND array_length(column_aliases, 1) > 0
      AND array_to_string(column_aliases, '') != ''
      THEN jsonb_build_object(
      'columns', to_jsonb(columns),
      'aliases', to_jsonb(column_aliases)
      )
      ELSE jsonb_build_object(
      'columns', to_jsonb(columns)
      )
      END
      )
      WHERE columns IS NOT NULL
      AND array_length(columns, 1) > 0;
    </sql>
  </changeSet>

  <changeSet id="12-3-migrate-data-export-template-to-schema" author="hdosprazeres" context="schema-change">
    <sql>
      UPDATE data_export_template
      SET schema = jsonb_build_object(
      'material-sample',
      CASE
      WHEN column_aliases IS NOT NULL
      AND array_length(column_aliases, 1) > 0
      AND array_to_string(column_aliases, '') != ''
      THEN jsonb_build_object(
      'columns', to_jsonb(columns),
      'aliases', to_jsonb(column_aliases)
      )
      ELSE jsonb_build_object(
      'columns', to_jsonb(columns)
      )
      END
      )
      WHERE columns IS NOT NULL
      AND array_length(columns, 1) > 0;
    </sql>
  </changeSet>

  <changeSet id="12-4-drop-legacy-columns-data-export" author="hdosprazeres" context="schema-change">
    <dropColumn columnName="columns" tableName="data_export"/>
    <dropColumn columnName="column_aliases" tableName="data_export"/>
    <dropColumn columnName="column_functions" tableName="data_export"/>
  </changeSet>

  <changeSet id="12-5-drop-legacy-columns-data-export-template" author="hdosprazeres" context="schema-change">
    <dropColumn columnName="columns" tableName="data_export_template"/>
    <dropColumn columnName="column_aliases" tableName="data_export_template"/>
    <dropColumn columnName="column_functions" tableName="data_export_template"/>
  </changeSet>
</databaseChangeLog>
